---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - README.md
  - CHANGELOG.md
autonomous: true

must_haves:
  truths:
    - "Plugin manifest exists at .claude-plugin/plugin.json"
    - "Manifest declares /pl namespace via name field"
    - "Marketplace catalog enables GitHub installation"
    - "README documents installation and usage"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin identity and namespace"
      contains: '"name": "pl"'
    - path: ".claude-plugin/marketplace.json"
      provides: "Distribution catalog"
      contains: '"source": "./"'
    - path: "README.md"
      provides: "User documentation"
      min_lines: 20
    - path: "CHANGELOG.md"
      provides: "Version history"
      contains: "1.0.0"
  key_links:
    - from: ".claude-plugin/marketplace.json"
      to: ".claude-plugin/plugin.json"
      via: "source field pointing to plugin root"
      pattern: '"source":\\s*"\\.\\/?"'
---

<objective>
Create the foundational plugin infrastructure: manifest, marketplace catalog, and documentation.

Purpose: Establish the plugin identity with `/pl` namespace and enable distribution via GitHub.
Output: Valid plugin manifest, marketplace catalog for installation, README with usage instructions, CHANGELOG for versioning.
</objective>

<execution_context>
@/Users/deprasny/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deprasny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin manifest and marketplace catalog</name>
  <files>.claude-plugin/plugin.json, .claude-plugin/marketplace.json</files>
  <action>
Create the `.claude-plugin/` directory and add:

1. **plugin.json** - Plugin manifest with:
   - `name`: "pl" (this creates the /pl: namespace)
   - `version`: "1.0.0"
   - `description`: "Potenlab team toolkit for Claude Code"
   - `author`: { "name": "Potenlab" }
   - `homepage`: "https://github.com/potenlab/marketplace-potenlab"
   - `repository`: "https://github.com/potenlab/marketplace-potenlab"
   - `license`: "MIT"
   - `keywords`: ["potenlab", "toolkit", "team"]

2. **marketplace.json** - Marketplace catalog with:
   - `name`: "potenlab-marketplace"
   - `owner`: { "name": "Potenlab" }
   - `metadata`: { "description": "Official Potenlab Claude Code plugins", "version": "1.0.0" }
   - `plugins`: array with single entry pointing to "./" with name "pl"

IMPORTANT: Use valid JSON (no trailing commas, no comments). The plugin name "pl" is what creates the /pl: command namespace automatically.
  </action>
  <verify>
Verify JSON syntax is valid:
- `cat .claude-plugin/plugin.json | python3 -m json.tool`
- `cat .claude-plugin/marketplace.json | python3 -m json.tool`
Both should parse without errors.
  </verify>
  <done>
- `.claude-plugin/plugin.json` exists with `"name": "pl"` and version 1.0.0
- `.claude-plugin/marketplace.json` exists with plugin entry pointing to "./"
- Both files are valid JSON (parseable)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create README and CHANGELOG</name>
  <files>README.md, CHANGELOG.md</files>
  <action>
Create documentation files at plugin root:

1. **README.md** with:
   - Title: "Potenlab Claude Code Marketplace"
   - Brief description of what the plugin provides
   - Installation section with commands:
     - `claude plugin marketplace add potenlab/marketplace-potenlab`
     - `claude plugin install pl@potenlab-marketplace`
   - Available Commands section listing /pl:help and /pl:init (note: coming in next plan)
   - Requirements section (Claude Code version 1.0.33+)
   - Contributing section (brief, internal team only)
   - License (MIT)

2. **CHANGELOG.md** with:
   - Header explaining the format
   - [1.0.0] - YYYY-MM-DD section with:
     - Added: Initial plugin infrastructure
     - Added: /pl:help command
     - Added: /pl:init command

Use today's date (2025-01-24) in CHANGELOG.
  </action>
  <verify>
- `test -f README.md && echo "README exists"`
- `test -f CHANGELOG.md && echo "CHANGELOG exists"`
- `grep -q "pl:help" README.md && echo "Commands documented"`
- `grep -q "1.0.0" CHANGELOG.md && echo "Version documented"`
  </verify>
  <done>
- README.md exists with installation instructions and command list
- CHANGELOG.md exists with 1.0.0 entry
- Both files are at plugin root (not inside .claude-plugin/)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Directory structure matches expected layout:
   ```
   marketplace-potenlab/
   ├── .claude-plugin/
   │   ├── plugin.json
   │   └── marketplace.json
   ├── README.md
   └── CHANGELOG.md
   ```

2. JSON files are valid and parseable
3. README contains installation instructions
4. Manifest declares "pl" as the plugin name
</verification>

<success_criteria>
- Plugin manifest exists and is valid JSON
- Marketplace catalog exists and references the plugin
- README documents installation and available commands
- CHANGELOG tracks version 1.0.0
- All files at correct locations (manifest in .claude-plugin/, docs at root)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
